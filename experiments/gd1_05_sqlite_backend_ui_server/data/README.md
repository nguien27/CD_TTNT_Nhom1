# Data GĐ1-05

- `nhan_vien_chuan.csv`: 100.000 dòng được rút gọn từ dữ liệu thành viên đóng góp, chỉ giữ các trường phù hợp core schema GĐ1-04.
- `don_vi_nguon.csv`: danh mục đơn vị gốc dùng làm metadata/tham chiếu. File này KHÔNG chứa YES/NO.
- `business_rules_mock.csv`: bộ quy tắc giả lập tách riêng khỏi danh mục đơn vị. `ket_qua` chỉ nằm ở Business Rule và mọi căn cứ đều đánh dấu `[MOCK]`.

Không coi các rule này là quy định pháp lý thực tế.
